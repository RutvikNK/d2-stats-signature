import React from 'react';
import './Header.css';

const Header = () => {
  return (
    <header className="header">
      <h1 className="site-title">Destiny 2 Sandbox Tracker</h1>
    </header>
  );
};

export default Header;
